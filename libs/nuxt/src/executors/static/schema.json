{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Static executor",
  "description": "",
  "type": "object",
  "properties": {
    "browserTarget": {
      "type": "string",
      "description": "Target to generate.",
      "pattern": "^[^:\\s]+:[^:\\s]+(:[^\\s]+)?$"
    }
  },
  "required": ["browserTarget"],
  "additionalProperties": false
}
